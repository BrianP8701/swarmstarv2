import { RootMutationResolvers } from "../../generated/graphql";

export const RootMutation: RootMutationResolvers = {
  memoryMutation: async (_, __) => {
    return {}
  },
  swarmMutation: async (_, __) => {
    return {}
  },
}
