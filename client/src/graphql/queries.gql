query FetchUser {
  user {
    ...User
  }
}

query FetchSwarm($swarmId: ID!) {
  swarm(id: $swarmId) {
    ...Swarm
  }
}

query FetchChat($chatId: ID!) {
  chat(id: $chatId) {
    ...Chat
  }
}

query FetchActionGraph {
  actionGraph {
    ...ActionGraph
  }
}
